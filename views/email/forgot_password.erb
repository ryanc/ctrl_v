Hi <%= @user.name || @user.username %>,

Someone (presumably you) has initiated a password reset. Please click the link
below to reset your password.

<%= password_reset_url(@user) %> 

Username: <%= @user.username %>
IP Address: <%= @ip_addr %>

If you have not requested this operation, just ignore the email and the password
reset will expire.
